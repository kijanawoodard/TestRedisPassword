# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: test-redis-password
services:  
  app:
    language: dotnet
    project: ./TestRedisPassword.AppHost/TestRedisPassword.AppHost.csproj
    host: containerapp
